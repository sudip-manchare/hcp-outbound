# Public backend origin, without /api or a trailing slash.
# For local development, copy this file to .env.local.
# For Docker, pass this value with --build-arg NEXT_PUBLIC_API_URL=...
NEXT_PUBLIC_API_URL=http://localhost:8000
